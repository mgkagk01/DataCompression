#pragma once
#include<iostream>
#include<math.h>
#include<vector>
void printData(int source[], int const lenSource);
int bin2dec(int binArray[], int size);
void dec2bin(int decNum, int binArryay[], int size);
void printCompressed(std::vector<bool> compressed, int const lenSource);
